import { notFound,redirect } from "next/navigation";import { getStaffSession } from "@/lib/cms/admin-stories";import { getAdminStory,listMediaAssets } from "@/lib/cms/admin-queries";import StoryEditor from "@/components/admin/stories/StoryEditor";export default async function EditStory({params}:{params:Promise<{id:string}>}){if(!await getStaffSession())redirect("/admin/login");const {id}=await params,story=await getAdminStory(id);if(!story)notFound();return <StoryEditor story={story} media={await listMediaAssets()}/>}
